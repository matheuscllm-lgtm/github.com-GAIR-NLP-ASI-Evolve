# asi-core runs on the Python standard library alone (3.10+).
# Everything below is OPTIONAL, only for production / LLM-backed agents:

pyyaml>=6.0        # EvolveConfig.from_yaml
# numpy>=1.24      # vectorised engineers
# faiss-cpu>=1.7   # CognitionStore embedding backend
# anthropic>=0.40  # LLM-backed Researcher/Analyzer (Claude)
